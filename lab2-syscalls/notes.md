# Task1 

## strace
strace - утилита просмотра следа системных вызовов и *сигналов*
Он перехватывает *как-то* системные вызовы, которые делает процесс
и выводит их в stderr. В stdout пойдёт вывод программы.

## Вывод strace 
- execve - запускает программу, которая


## write(2)
